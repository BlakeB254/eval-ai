export enum ScoringType {
  NUMERIC = 'numeric',
  LETTER = 'letter',
  TIER = 'tier',
  RANK = 'rank',
  PASS_FAIL = 'pass_fail',
}

export enum FieldType {
  TEXT = 'text',
  TEXTAREA = 'textarea',
  NUMBER = 'number',
  EMAIL = 'email',
  PHONE = 'phone',
  SELECT = 'select',
  MULTISELECT = 'multiselect',
  CHECKBOX = 'checkbox',
  RADIO = 'radio',
  DATE = 'date',
  FILE = 'file',
  URL = 'url',
}
